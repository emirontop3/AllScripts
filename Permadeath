if _G.Perma == true then
    local Players = game:GetService("Players")
    local RunService = game:GetService("RunService")

    local player = Players.LocalPlayer
    local activeConnection = nil

    local function startPermaDeath(character)
        local root = character:WaitForChild("HumanoidRootPart")
        local humanoid = character:WaitForChild("Humanoid")
        
        if activeConnection then activeConnection:Disconnect() end

        -- 1. DURUMLARI KAPAT (Karakterin yere yığılmasını önler)
        humanoid:SetStateEnabled(Enum.HumanoidStateType.Dead, false)
        humanoid:SetStateEnabled(Enum.HumanoidStateType.FallingDown, false)
        humanoid:SetStateEnabled(Enum.HumanoidStateType.Ragdoll, false)
        
        -- Çarpışmayı kapatmak fling riskini %99 azaltır
        root.CanCollide = false

        -- 2. DOĞAL HIZ DÖNGÜSÜ
        activeConnection = RunService.Heartbeat:Connect(function()
            if not root or not root.Parent then
                if activeConnection then activeConnection:Disconnect() end
                return
            end

            -- Hızı olduğu gibi bırakıyoruz (oldV = oldV)
            -- Sadece çok küçük bir 'yaşam sinyali' (0.001) ekliyoruz ki 
            -- oyun motoru karakteri "hareketsiz/ölü" sanıp silmesin.
            local currentV = root.AssemblyLinearVelocity
            root.AssemblyLinearVelocity = currentV + Vector3.new(0, 0.001, 0)
            
            -- Dönüş hızını sıfırlayarak titremeyi engeller
            root.AssemblyAngularVelocity = Vector3.new(0, 0, 0)
        end)
    end

    -- 3. KARAKTER YENİLENİNCE ÇALIŞTIR
    player.CharacterAdded:Connect(function(newChar)
        task.wait(1.5)
        startPermaDeath(newChar)
    end)

    if player.Character then
        task.spawn(function() startPermaDeath(player.Character) end)
    end

    print("--- PERMA DEATH: NORMAL HIZ AKTİF ---")
end
